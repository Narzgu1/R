# Встановлення та завантаження пакету DiagrammeR
if (!requireNamespace("DiagrammeR", quietly = TRUE)) {
  install.packages("DiagrammeR")
}
library(DiagrammeR)

# Створення діаграми станів
diagram <- "
stateDiagram-v2
    [*] --> Запит_на_розміщення_подано
    Запит_на_розміщення_подано --> Місце_зарезервовано : Подання_запиту_батьками
    Місце_зарезервовано --> Медичний_огляд_пройдено : Затвердження_запиту_завідуючою
    Медичний_огляд_пройдено --> Дитина_зарахована : Надання_медичних_довідок_батьками
    Дитина_зарахована --> Місце_звільнено : Ненадання_медичних_довідок_протягом_двох_тижнів
    Місце_зарезервовано --> Місце_звільнено : Ненадання_медичних_довідок_протягом_двох_тижнів
    Дитина_зарахована --> [*]
"

# Відображення діаграми
grViz(diagram)